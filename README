Confirmed to work with the following setup.
* Python 3.6
* PyQt5
* Windows 10

From pip:
pyqt5

xlrd

Standalone package:
cx_Freeze from pip
Compile windows installer with python setup.py bdist_msi

Note that this has been confirmed to work with the listed version numbers.


= Usage =
TestStatisticsCalculator is an example plugin, but it does not do any meaningful calculations. Copy it and its
folder and rename to create your own plugin.

